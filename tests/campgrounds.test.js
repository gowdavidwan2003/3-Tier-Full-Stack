describe('Simulated test suite', () => {
  it('should pass unit test 1', (done) => {
    setTimeout(() => {
      expect(true).toBe(true);
      done();
    }, 1000); // 1 seconds delay
  });

  it('should pass unit test 2', (done) => {
    setTimeout(() => {
      expect(true).toBe(true);
      done();
    }, 1500); // 1.5 seconds delay
  });

  it('should pass integration test', (done) => {
    setTimeout(() => {
      expect(true).toBe(true);
      done();
    }, 2000); // 2 seconds delay
  });

  it('should pass integration test', (done) => {
    setTimeout(() => {
      expect(true).toBe(true);
      done();
    }, 3000); // 3 seconds delay
  });

  it('should pass integration test', (done) => {
    setTimeout(() => {
      expect(true).toBe(true);
      done();
    }, 2000); // 2 seconds delay
  });
});

describe('Simulated test suite-1', () => {
  it('should pass unit test 1', (done) => {
    setTimeout(() => {
      expect(true).toBe(true);
      done();
    }, 1000); // 1 seconds delay
  });

  it('should pass unit test 2', (done) => {
    setTimeout(() => {
      expect(true).toBe(true);
      done();
    }, 1500); // 1.5 seconds delay
  });


});

describe('Simulated test suite-2', () => {
  it('should pass unit test 1', (done) => {
    setTimeout(() => {
      expect(true).toBe(true);
      done();
    }, 1000); // 1 seconds delay
  });

  it('should pass unit test 2', (done) => {
    setTimeout(() => {
      expect(true).toBe(true);
      done();
    }, 1500); // 1.5 seconds delay
  });

  it('should pass integration test', (done) => {
    setTimeout(() => {
      expect(true).toBe(true);
      done();
    }, 2000); // 2 seconds delay
  });

  it('should pass integration test', (done) => {
    setTimeout(() => {
      expect(true).toBe(true);
      done();
    }, 3000); // 3 seconds delay
  });
});
